name: identyquest_v2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.0.0
  flutter_localizations:
    sdk: flutter
  intl: any
  google_fonts: ^6.1.0
  go_router: ^13.0.0
  http: ^1.2.0
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  flutter_bloc: ^8.1.3
  table_calendar: ^3.0.8
  font_awesome_flutter: ^10.7.0
  flutter_svg: ^2.0.9
  path_provider: ^2.1.2
  multi_select_flutter: ^4.1.3
  image_picker: ^1.0.4
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  geolocator: ^11.0.0
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/ 
    - assets/

  # 👇 Pour générer automatiquement les fichiers de traduction si tu ajoutes des fichiers ARB plus tard
  generate: true

# 👇 Config pour générer les icônes
flutter_launcher_icons:
  android: true
  ios: true
  # adaptive_icon_foreground: "assets/icons/icon_foreground.png"
  # adaptive_icon_background: "assets/icons/icon_background.png"
  image_path_ios: "assets/icons/iconev1.png" # ou v2 pour iOS normal
  min_sdk_android: 21
